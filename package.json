{
  "name": "smart-cloud-inventory-sales-analytics-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Smart Cloud Inventory & Sales Analytics Platform",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev --workspace frontend\" \"npm run dev:api\" \"npm run dev:electron\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:api": "python -m uvicorn app.main:app --reload --app-dir backend",
    "dev:electron": "wait-on http://localhost:5173 && npm run electron --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
